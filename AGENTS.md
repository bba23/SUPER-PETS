# Repo Instructions

- For exe, jar, and other executable outputs, every time you change them, bump the version by `0.0.1`.
- For every new release, provide these jar assets:
  - AI-only mod jar.
  - Base-mod-only jar.
  - Base mod plus AI jar.
